# 🛡️ GEMINI COLLABORATION & MAINTENANCE PROTOCOL

> [!CAUTION]
> ### ⚠️ هشدار بسیار حیاتی برای کلیه توسعه‌دهندگان و دستیاران هوش مصنوعی:
> **قبل از اعمال هرگونه تغییر یا ویرایش در این پروژه، ابتدا حتماً دستور زیر را اجرا کنید:**
> ```bash
> git pull origin main
> ```
> **دلیل:** همکاران و توسعه‌دهندگان دیگر به صورت همزمان بر روی این پروژه در حال فعالیت هستند. عدم پول کردن قبل از تغییرات منجر به تعارض (Git Merge Conflict) و از دست رفتن تغییرات خواهد شد.

---

## 🏢 اطلاعات شناسنامه‌ای پروژه

- **نام برند تجاری:** نوآوران پنجره سپاهان (Noavaran Panjereh Sepahan)
- **زمینه تخصصی:** طراحی، مهندسی محاسبات، تولید صنعتی و اجرای نماهای مدرن شیشه‌ای (کرتین‌وال / لامل، فریم‌لس)، درب و پنجره‌های اختصاصی دوجداره و ترمال‌بریک، پنل‌های کامپوزیت آلومینیوم، چوب ترموود و حفاظ/نرده استیل و شیشه‌ای.
- **زیرساخت فنی:** Next.js 14/15 App Router، React، TypeScript، Tailwind CSS، Lucide Icons.
- **مخزن گیت‌هاب:** `https://github.com/shatinz/noavaran-panjereh-sepahan`
- **دامنه استقرار Vercel:** `https://noavaranpanjereh.vercel.app`
- **داشبورد مدیریت:** آدرس `/admin`

---

## 🔑 اطلاعات ورود پیش‌فرض پنل مدیریت (Admin)

- **نام کاربری پیش‌فرض:** `admin`
- **رمز عبور پیش‌فرض:** `noavaran2026`
*(توسعه‌دهندگان می‌توانند متغیرهای `ADMIN_USERNAME` و `ADMIN_PASSWORD` را در فایل `.env.local` یا پنل Vercel سفارشی‌سازی کنند).*

---

## 📝 تاریخچه تغییرات مهم (Important Changes Log)

| تاریخ | نویسنده / ایجنت | خلاصه تغییرات اعمال شده | وضعیت |
| :--- | :---: | :--- | :---: |
| ۲۰۲۶-۰۹-۱۷ | Antigravity AI | استخراج کامل ۱۰۰٪ داده‌های arvinpanjereh.com (خدمات، مقالات، ۵۵ پروژه، مشخصات تماس). تدوین سند `phases.md` و استقرار `gemini.md`. | ✅ تکمیل شد |

---

## 🔁 دفترچه حل مشکلات پرتکرار (Recurring Problem Solutions)
*این بخش برای مسائلی است که بیش از ۳ بار تکرار شده و راه‌حل قطعی آن‌ها مستند شده است:*

### ۱. مشکل انکودینگ کاراکترهای فارسی در کنسول ویندوز (UnicodeEncodeError cp1252)
- **علت:** به صورت پیش‌فرض شل ویندوز از کدپیج ۱۲۵۲ استفاده می‌کند و هنگام چاپ کاراکترهای فارسی با `print()` خطای `charmap` رخ می‌دهد.
- **راه‌حل:** در ابتدای تمام اسکریپت‌های پایتون دستور `sys.stdout.reconfigure(encoding='utf-8')` درج شود یا داده‌ها به فایل UTF-8 نوشته شوند.

### ۲. تایم‌اوت و هنگ دستورات شبکه در پاورشل ویندوز
- **علت:** دستورات `Invoke-WebRequest` بدون `-UseBasicParsing` به کتابخانه قدیمی اینترنت اکسپلورر متصل شده و هنگ می‌کنند.
- **راه‌حل:** همواره از `curl.exe` مستقیم با پارامترهای `--connect-timeout 5 --max-time 15` استفاده شود.

### ۳. کلیپینگ و جابجایی تصاویر در صفحات ریسپانسیو RTL در موتور WebKit
- **علت:** استفاده از ترنسفورم‌های انیمیشن یا رپرهای چندلایه flexbox در دایرکشن RTL.
- **راه‌حل:** استفاده از ساختار تمیز بلوکی `block mx-auto w-full h-full object-contain` همراه با `overflow-x: clip` روی ریشه صفحه.
